# Never commit secrets
config.ini

# Python
*.pyc
__pycache__/

# Virtual environment (managed by uv)
.venv/

# Lambda build artifacts
build/
lambda.zip

# Terraform state and local overrides (never commit state files)
terraform/**/.terraform/
terraform/**/*.tfstate
terraform/**/*.tfstate.backup
terraform/**/terraform.tfvars